<h2>New book</h2>
<div>
  <%= form_with model: book do |f| %>
  <div>Title <%= f.text_field :title %></div>
  <div>Opinion<%= f.text_area :body  %></div>
  <%= f.submit 'Creat Book',class:"btn btn-lg btn-success" %>
  <% end %>
</div>
